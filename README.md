# servlet-simple-servlet

```
git clone https://github.com/WO8TimeSpace175ZERO2/servlet-simple-servlet.git

cd servlet-simple-servlet

mvn clean package -DskipTests

```

targetフォルダにビルドされたservlet-simple-servlet.warをデプロイ

